# TODO
- use python or something like danfo.js for data processing. Think through this, ideally we would use a library that can handle larger datasets and has access to basic statistical functions like linear regression.
- Make each node robust and able to fix errors before terminating. Right now a failed node stays around and generates another node with the fix.